
% \section{Distributed Signatures}
% \label{app:dist_signing}
% %
% \figref{fig:protocol:dist_sign} describes the distributed key generation and signing protocols for our set homomorphic signature scheme. Since the protocol simply uses the protocols of \citet{algesheimer2002efficient} to compute a shared modulus, and \citet{catalano2000computing} to invert values module a shared modulus, we refer to their works for a full security proof. 

\input{dss_protocol.tex}